# ============================================
# DEPENDENCIAS ACTUALIZADAS CON CORRECCIONES DE SEGURIDAD
# ============================================

# Core Framework (versiones seguras actualizadas)
Flask>=3.0.0
Werkzeug>=3.0.0
gunicorn>=21.2.0

# Database
Flask-SQLAlchemy>=3.1.0
psycopg2-binary>=2.9.9

# Authentication & Sessions
Flask-Login>=0.6.3

# Security Extensions (NUEVAS)
Flask-WTF>=1.2.0           # CSRF Protection
Flask-Limiter>=3.5.0       # Rate Limiting
Flask-Talisman>=1.1.0      # HTTPS/Security Headers

# Environment & Configuration
python-dotenv>=1.0.0

# HTTP Requests (actualizado)
requests>=2.32.0

# Security Scanning Tools (NUEVAS)
safety>=3.0.0              # Escaneo de vulnerabilidades
pip-audit>=2.6.0           # Auditoría de dependencias

# Logging & Monitoring (OPCIONAL pero recomendado)
python-json-logger>=2.0.7  # Structured logging
sentry-sdk[flask]>=1.40.0  # Error tracking (opcional)

# Testing de Seguridad (desarrollo)
bandit>=1.7.5              # Security linting
pytest>=7.4.3              # Testing framework
pytest-cov>=4.1.0          # Coverage
